{{#sketchClass.deleted}}
<div class="alert alert-warn" style="margin-bottom:10px;">
  This sketch was created using the "{{sketchClass.name}}" template, which is
  no longer available. You will not be able to copy this sketch or make new
  sketches of this type.
</div>
{{/sketchClass.deleted}}

<div class="reportSection size {{^DIAM_OK}}warning{{/DIAM_OK}}">
  <h4>Size</h4>
  <p class="large">
    This protected area has an area of <strong>{{SQ_MILES}} square miles</strong>.
  </p>
  <p>
    Diameter (on smallest side): {{DIAM}}. Recommended > {{MIN_DIAM}}
  </p>
</div>

{{#hasDescription}}
<div class="reportSection">
  <h4>Description</h4>
  <p>{{description}}</p>
</div>
{{/hasDescription}}
